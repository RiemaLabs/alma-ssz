diff --git a/sszgen/generator/hash.go b/sszgen/generator/hash.go
index 1a48036..b39efa3 100755
--- a/sszgen/generator/hash.go
+++ b/sszgen/generator/hash.go
@@ -244,25 +244,7 @@ func (v *Value) hashTreeRoot(name string, appendBytes bool) string {
 
 func (v *Value) hashTreeRootContainer(start bool) string {
 	if !start {
-		tmpl := `{{ if .check }}if ::.{{.name}} == nil {
-			::.{{.name}} = new({{ref .obj}})
-		}
-		{{ end }}if err = ::.{{.name}}.HashTreeRootWith(hh); err != nil {
-			return
-		}`
-		// validate only for fixed structs
-		check := v.isFixed()
-		if v.isListElem() {
-			check = false
-		}
-		if v.noPtr {
-			check = false
-		}
-		return execTmpl(tmpl, map[string]interface{}{
-			"name":  v.name,
-			"obj":   v,
-			"check": check,
-		})
+		return fmt.Sprintf("if err = ::.%s.HashTreeRootWith(hh); err != nil {\n\treturn\n}", v.name)
 	}
 
 	out := []string{}
